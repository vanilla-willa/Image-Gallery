- if user_signed_in?
    %p#intro
        Hello
        = current_user.name
        %br/
        %span
            Share an image that has made your day! :)
- else
    #p#intro
        What do you see?
        %br
        %span
            Share an image that has made your day! :)

#posts
    - @posts.each do |post|
        .post
            .post_image
                = link_to(image_tag post.image.url(:small))
            .post_content
                .title
                    %h1= link_to post.title, post
                .data.clearfix
                    %p.username
                        Shared by
                        = post.user.name
                    %p.buttons
                        %span
                            %i.fa.fa-comments-o
                            = post.comments.count
                        %span
                            %i.fa.fa-thumbs-o-up
                            = post.get_likes.size