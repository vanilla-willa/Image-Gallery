- @posts.each do |post|
    = link_to(image_tag post.image.url(:small))
    %h1= link_to post.title, post
    %p
        = post.comments.count
        Comments
    %p
        = post.get_likes.size
        Likes

= link_to "Upload Image", new_post_path